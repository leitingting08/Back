#include <stdio.h>
int is( int number );
int main(void){
	int count,i,sum;
	count = 0;
	sum = 0;
	for(i=100;i<=999;i++)
	if(is(i) = 1){
		count++;
		sum = sum + i; 
	}
	printf("count=%d,sum=%d\n",count,sums);
	return 0;
}
